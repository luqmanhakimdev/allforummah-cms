diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-6OA77UM7.cjs b/node_modules/@sonicjs-cms/core/dist/chunk-6OA77UM7.cjs
index a54eb33..0f978da 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-6OA77UM7.cjs
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-6OA77UM7.cjs
@@ -3839,8 +3839,7 @@ function renderLoginPage(data, demoLoginActive = false) {
             <!-- Links -->
             <div class="mt-6 text-center">
               <p class="text-sm text-zinc-400">
-                Don't have an account?
-                <a href="/auth/register" class="font-semibold text-white hover:text-zinc-300 transition-colors">Create one here</a>
+                Need an account? Contact an administrator.
               </p>
             </div>
           </div>
@@ -7684,24 +7683,30 @@ authRoutes.post(
       `);
       }
       const passwordHash = await chunkE7E3NL3Y_cjs.AuthManager.hashPassword(password);
-      const role = isFirstUser ? "admin" : "viewer";
+      const role = isFirstUser ? "admin" : "editor";
       const userId = crypto.randomUUID();
       const now = /* @__PURE__ */ new Date();
-      await db.prepare(`
+      const nowSec = Math.floor(now.getTime() / 1e3);
+      // Better Auth sign-in/email requires an auth_account credential row — create it alongside auth_user
+      await db.batch([
+        db.prepare(`
       INSERT INTO auth_user (id, email, first_name, last_name, password_hash, role, is_active, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
     `).bind(
-        userId,
-        normalizedEmail,
-        firstName,
-        lastName,
-        passwordHash,
-        role,
-        1,
-        // is_active
-        now.getTime(),
-        now.getTime()
-      ).run();
+          userId,
+          normalizedEmail,
+          firstName,
+          lastName,
+          passwordHash,
+          role,
+          1,
+          // is_active
+          now.getTime(),
+          now.getTime()
+        ),
+        db.prepare(`INSERT OR IGNORE INTO auth_account (id, user_id, account_id, provider_id, password, created_at, updated_at)
+          VALUES (?, ?, ?, 'credential', ?, ?, ?)`).bind(`cred-${userId}`, userId, userId, passwordHash, nowSec, nowSec)
+      ]);
       const profileConfig = getUserProfileConfig();
       if (profileConfig) {
         const regFields = getRegistrationFields();
@@ -16153,26 +16158,34 @@ userRoutes.post("/users/new", async (c) => {
     }
     const passwordHash = await chunkE7E3NL3Y_cjs.AuthManager.hashPassword(password);
     const userId = crypto.randomUUID();
-    const createStmt = db.prepare(`
+    const nowMs = Date.now();
+    const nowSec = Math.floor(nowMs / 1e3);
+    // Better Auth sign-in requires auth_account credential row alongside auth_user
+    await db.batch([
+      db.prepare(`
       INSERT INTO auth_user (
         id, email, first_name, last_name, phone, bio,
         password_hash, role, is_active, email_verified, created_at, updated_at
       ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-    `);
-    await createStmt.bind(
-      userId,
-      email,
-      firstName,
-      lastName,
-      phone,
-      bio,
-      passwordHash,
-      role,
-      isActive ? 1 : 0,
-      emailVerified ? 1 : 0,
-      Date.now(),
-      Date.now()
-    ).run();
+    `).bind(
+        userId,
+        email,
+        firstName,
+        lastName,
+        phone,
+        bio,
+        passwordHash,
+        role,
+        isActive ? 1 : 0,
+        emailVerified ? 1 : 0,
+        nowMs,
+        nowMs
+      ),
+      db.prepare(`INSERT OR IGNORE INTO auth_account (id, user_id, account_id, provider_id, password, created_at, updated_at)
+        VALUES (?, ?, ?, 'credential', ?, ?, ?)`).bind(`cred-${userId}`, userId, userId, passwordHash, nowSec, nowSec)
+    ]);
+    // Sync RBAC role docs (portal:access); auth_user.role alone is not enough for /admin
+    await new chunkJCBDXEY2_cjs.RbacService(db).setUserRoles(userId, [role]);
     if (sanitizedRegistrationCustom !== null && Object.keys(sanitizedRegistrationCustom).length > 0) {
       await writeProfileData(db, userId, { custom: sanitizedRegistrationCustom }, user.userId);
     }
@@ -16429,6 +16442,8 @@ userRoutes.put("/users/:id", async (c) => {
       Date.now(),
       userId
     ).run();
+    // Keep rbac_user_roles in sync so portal:access and requireRole stay consistent
+    await new chunkJCBDXEY2_cjs.RbacService(db).setUserRoles(userId, [role]);
     if (newPassword) {
       const passwordHash = await chunkE7E3NL3Y_cjs.AuthManager.hashPassword(newPassword);
       const updatePasswordStmt = db.prepare(`
@@ -22360,6 +22375,7 @@ function renderLogConfigPage(data) {
 // src/routes/admin-logs.ts
 var adminLogsRoutes = new hono.Hono();
 adminLogsRoutes.use("*", chunkE7E3NL3Y_cjs.requireAuth());
+adminLogsRoutes.use("*", chunkE7E3NL3Y_cjs.requireRole(["admin"]));
 adminLogsRoutes.get("/", async (c) => {
   try {
     const user = c.get("user");
@@ -26587,6 +26603,7 @@ function renderDatabaseToolsSettings(settings) {
 // src/routes/admin-settings.ts
 var adminSettingsRoutes = new hono.Hono();
 adminSettingsRoutes.use("*", chunkE7E3NL3Y_cjs.requireAuth());
+adminSettingsRoutes.use("*", chunkE7E3NL3Y_cjs.requireRole(["admin"]));
 function getMockSettings(user) {
   return {
     general: {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-7P5OCUPS.js b/node_modules/@sonicjs-cms/core/dist/chunk-7P5OCUPS.js
index 2aeb370..7f615e2 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-7P5OCUPS.js
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-7P5OCUPS.js
@@ -3837,8 +3837,7 @@ function renderLoginPage(data, demoLoginActive = false) {
             <!-- Links -->
             <div class="mt-6 text-center">
               <p class="text-sm text-zinc-400">
-                Don't have an account?
-                <a href="/auth/register" class="font-semibold text-white hover:text-zinc-300 transition-colors">Create one here</a>
+                Need an account? Contact an administrator.
               </p>
             </div>
           </div>
@@ -7682,24 +7681,30 @@ authRoutes.post(
       `);
       }
       const passwordHash = await AuthManager.hashPassword(password);
-      const role = isFirstUser ? "admin" : "viewer";
+      const role = isFirstUser ? "admin" : "editor";
       const userId = crypto.randomUUID();
       const now = /* @__PURE__ */ new Date();
-      await db.prepare(`
+      const nowSec = Math.floor(now.getTime() / 1e3);
+      // Better Auth sign-in/email requires an auth_account credential row — create it alongside auth_user
+      await db.batch([
+        db.prepare(`
       INSERT INTO auth_user (id, email, first_name, last_name, password_hash, role, is_active, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
     `).bind(
-        userId,
-        normalizedEmail,
-        firstName,
-        lastName,
-        passwordHash,
-        role,
-        1,
-        // is_active
-        now.getTime(),
-        now.getTime()
-      ).run();
+          userId,
+          normalizedEmail,
+          firstName,
+          lastName,
+          passwordHash,
+          role,
+          1,
+          // is_active
+          now.getTime(),
+          now.getTime()
+        ),
+        db.prepare(`INSERT OR IGNORE INTO auth_account (id, user_id, account_id, provider_id, password, created_at, updated_at)
+          VALUES (?, ?, ?, 'credential', ?, ?, ?)`).bind(`cred-${userId}`, userId, userId, passwordHash, nowSec, nowSec)
+      ]);
       const profileConfig = getUserProfileConfig();
       if (profileConfig) {
         const regFields = getRegistrationFields();
@@ -16151,26 +16156,34 @@ userRoutes.post("/users/new", async (c) => {
     }
     const passwordHash = await AuthManager.hashPassword(password);
     const userId = crypto.randomUUID();
-    const createStmt = db.prepare(`
+    const nowMs = Date.now();
+    const nowSec = Math.floor(nowMs / 1e3);
+    // Better Auth sign-in requires auth_account credential row alongside auth_user
+    await db.batch([
+      db.prepare(`
       INSERT INTO auth_user (
         id, email, first_name, last_name, phone, bio,
         password_hash, role, is_active, email_verified, created_at, updated_at
       ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
-    `);
-    await createStmt.bind(
-      userId,
-      email,
-      firstName,
-      lastName,
-      phone,
-      bio,
-      passwordHash,
-      role,
-      isActive ? 1 : 0,
-      emailVerified ? 1 : 0,
-      Date.now(),
-      Date.now()
-    ).run();
+    `).bind(
+        userId,
+        email,
+        firstName,
+        lastName,
+        phone,
+        bio,
+        passwordHash,
+        role,
+        isActive ? 1 : 0,
+        emailVerified ? 1 : 0,
+        nowMs,
+        nowMs
+      ),
+      db.prepare(`INSERT OR IGNORE INTO auth_account (id, user_id, account_id, provider_id, password, created_at, updated_at)
+        VALUES (?, ?, ?, 'credential', ?, ?, ?)`).bind(`cred-${userId}`, userId, userId, passwordHash, nowSec, nowSec)
+    ]);
+    // Sync RBAC role docs (portal:access); auth_user.role alone is not enough for /admin
+    await new RbacService(db).setUserRoles(userId, [role]);
     if (sanitizedRegistrationCustom !== null && Object.keys(sanitizedRegistrationCustom).length > 0) {
       await writeProfileData(db, userId, { custom: sanitizedRegistrationCustom }, user.userId);
     }
@@ -16427,6 +16440,8 @@ userRoutes.put("/users/:id", async (c) => {
       Date.now(),
       userId
     ).run();
+    // Keep rbac_user_roles in sync so portal:access and requireRole stay consistent
+    await new RbacService(db).setUserRoles(userId, [role]);
     if (newPassword) {
       const passwordHash = await AuthManager.hashPassword(newPassword);
       const updatePasswordStmt = db.prepare(`
@@ -22358,6 +22373,7 @@ function renderLogConfigPage(data) {
 // src/routes/admin-logs.ts
 var adminLogsRoutes = new Hono();
 adminLogsRoutes.use("*", requireAuth());
+adminLogsRoutes.use("*", requireRole(["admin"]));
 adminLogsRoutes.get("/", async (c) => {
   try {
     const user = c.get("user");
@@ -26585,6 +26601,7 @@ function renderDatabaseToolsSettings(settings) {
 // src/routes/admin-settings.ts
 var adminSettingsRoutes = new Hono();
 adminSettingsRoutes.use("*", requireAuth());
+adminSettingsRoutes.use("*", requireRole(["admin"]));
 function getMockSettings(user) {
   return {
     general: {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-BDQ55MWV.js b/node_modules/@sonicjs-cms/core/dist/chunk-BDQ55MWV.js
index 92af16d..1836430 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-BDQ55MWV.js
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-BDQ55MWV.js
@@ -745,8 +745,7 @@ var RbacService = class _RbacService {
           { resource: "document_type:*", verb: "read" },
           { resource: "document_type:*", verb: "create" },
           { resource: "document_type:*", verb: "update" },
-          { resource: "document_type:*", verb: "delete" },
-          { resource: "settings", verb: "read" }
+          { resource: "document_type:*", verb: "delete" }
         ]
       },
       {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-HSDJDWLV.js b/node_modules/@sonicjs-cms/core/dist/chunk-HSDJDWLV.js
index eedf8a8..74a50f8 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-HSDJDWLV.js
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-HSDJDWLV.js
@@ -1010,7 +1010,7 @@ function renderSidebar(currentPath, user, dynamicMenuItems) {
   const isAdmin = userRole === "admin";
   const isEditor = userRole === "editor";
   const canManageUsers = isAdmin;
-  const canAccessSettings = isAdmin || isEditor;
+  const canAccessSettings = isAdmin;
   const canAccessPlugins = isAdmin;
   const baseMenuItems = [
     {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-JCBDXEY2.cjs b/node_modules/@sonicjs-cms/core/dist/chunk-JCBDXEY2.cjs
index 711a190..9981032 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-JCBDXEY2.cjs
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-JCBDXEY2.cjs
@@ -747,8 +747,7 @@ var RbacService = class _RbacService {
           { resource: "document_type:*", verb: "read" },
           { resource: "document_type:*", verb: "create" },
           { resource: "document_type:*", verb: "update" },
-          { resource: "document_type:*", verb: "delete" },
-          { resource: "settings", verb: "read" }
+          { resource: "document_type:*", verb: "delete" }
         ]
       },
       {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-MI2YXCK4.cjs b/node_modules/@sonicjs-cms/core/dist/chunk-MI2YXCK4.cjs
index e858413..2f261f9 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-MI2YXCK4.cjs
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-MI2YXCK4.cjs
@@ -545,7 +545,7 @@ function renderCatalystSidebar(currentPath = "", user, dynamicMenuItems, isMobil
   const isAdmin = userRole === "admin";
   const isEditor = userRole === "editor";
   const canManageUsers = isAdmin;
-  const canAccessSettings = isAdmin || isEditor;
+  const canAccessSettings = isAdmin;
   const canAccessPlugins = isAdmin;
   let baseMenuItems = [
     {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-OUBUS74I.cjs b/node_modules/@sonicjs-cms/core/dist/chunk-OUBUS74I.cjs
index 74a5f1b..867d315 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-OUBUS74I.cjs
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-OUBUS74I.cjs
@@ -1012,7 +1012,7 @@ function renderSidebar(currentPath, user, dynamicMenuItems) {
   const isAdmin = userRole === "admin";
   const isEditor = userRole === "editor";
   const canManageUsers = isAdmin;
-  const canAccessSettings = isAdmin || isEditor;
+  const canAccessSettings = isAdmin;
   const canAccessPlugins = isAdmin;
   const baseMenuItems = [
     {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-SXC4HPK6.js b/node_modules/@sonicjs-cms/core/dist/chunk-SXC4HPK6.js
index eadbaf4..2faff52 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-SXC4HPK6.js
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-SXC4HPK6.js
@@ -543,7 +543,7 @@ function renderCatalystSidebar(currentPath = "", user, dynamicMenuItems, isMobil
   const isAdmin = userRole === "admin";
   const isEditor = userRole === "editor";
   const canManageUsers = isAdmin;
-  const canAccessSettings = isAdmin || isEditor;
+  const canAccessSettings = isAdmin;
   const canAccessPlugins = isAdmin;
   let baseMenuItems = [
     {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-VVYVFCML.js b/node_modules/@sonicjs-cms/core/dist/chunk-VVYVFCML.js
index f7377f0..3ff1158 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-VVYVFCML.js
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-VVYVFCML.js
@@ -1,28 +1,9 @@
 import { z } from 'zod';
 
 // src/services/auth-validation.ts
-async function isRegistrationEnabled(db) {
-  try {
-    const row = await db.prepare(
-      "SELECT data FROM documents WHERE slug = 'core-auth' AND type_id = 'plugin' AND tenant_id = 'default' AND is_current_draft = 1 AND deleted_at IS NULL"
-    ).first();
-    if (row?.data) {
-      const data = typeof row.data === "string" ? JSON.parse(row.data) : row.data;
-      const enabled = data?.settings?.registration?.enabled;
-      return enabled !== false && enabled !== 0;
-    }
-  } catch {
-  }
-  try {
-    const plugin = await db.prepare("SELECT settings FROM plugins WHERE id = ?").bind("core-auth").first();
-    if (plugin?.settings) {
-      const settings = JSON.parse(plugin.settings);
-      const enabled = settings?.registration?.enabled;
-      return enabled !== false && enabled !== 0;
-    }
-  } catch {
-  }
-  return true;
+async function isRegistrationEnabled(_db) {
+  // Public self-registration disabled — only admins create users via /admin/users
+  return false;
 }
 async function isFirstUserRegistration(db) {
   try {
diff --git a/node_modules/@sonicjs-cms/core/dist/chunk-XWVDSM2L.cjs b/node_modules/@sonicjs-cms/core/dist/chunk-XWVDSM2L.cjs
index 256f5fa..ba11e9c 100644
--- a/node_modules/@sonicjs-cms/core/dist/chunk-XWVDSM2L.cjs
+++ b/node_modules/@sonicjs-cms/core/dist/chunk-XWVDSM2L.cjs
@@ -3,28 +3,9 @@
 var zod = require('zod');
 
 // src/services/auth-validation.ts
-async function isRegistrationEnabled(db) {
-  try {
-    const row = await db.prepare(
-      "SELECT data FROM documents WHERE slug = 'core-auth' AND type_id = 'plugin' AND tenant_id = 'default' AND is_current_draft = 1 AND deleted_at IS NULL"
-    ).first();
-    if (row?.data) {
-      const data = typeof row.data === "string" ? JSON.parse(row.data) : row.data;
-      const enabled = data?.settings?.registration?.enabled;
-      return enabled !== false && enabled !== 0;
-    }
-  } catch {
-  }
-  try {
-    const plugin = await db.prepare("SELECT settings FROM plugins WHERE id = ?").bind("core-auth").first();
-    if (plugin?.settings) {
-      const settings = JSON.parse(plugin.settings);
-      const enabled = settings?.registration?.enabled;
-      return enabled !== false && enabled !== 0;
-    }
-  } catch {
-  }
-  return true;
+async function isRegistrationEnabled(_db) {
+  // Public self-registration disabled — only admins create users via /admin/users
+  return false;
 }
 async function isFirstUserRegistration(db) {
   try {
diff --git a/node_modules/@sonicjs-cms/core/dist/index.cjs b/node_modules/@sonicjs-cms/core/dist/index.cjs
index c71767d..f1e025d 100644
--- a/node_modules/@sonicjs-cms/core/dist/index.cjs
+++ b/node_modules/@sonicjs-cms/core/dist/index.cjs
@@ -1216,7 +1216,7 @@ function makeOnRegistrationCompleted(env) {
       if (setting?.value === "true") return;
     } catch {
     }
-    const user = await db.prepare("SELECT id, email, first_name FROM users WHERE id = ?").bind(payload.user.id).first();
+    const user = await db.prepare("SELECT id, email, first_name FROM auth_user WHERE id = ?").bind(payload.user.id).first();
     if (!user) {
       console.warn("[email-plugin] auth:registration:completed: user not found", {
         userId: payload.user.id
@@ -1288,7 +1288,7 @@ var DEFAULT_EXPIRY_MS = 60 * 60 * 1e3;
 function makeOnPasswordResetRequested(env) {
   const db = env.DB;
   return async (payload) => {
-    const user = await db.prepare("SELECT id, email, first_name FROM users WHERE id = ?").bind(payload.user.id).first();
+    const user = await db.prepare("SELECT id, email, first_name FROM auth_user WHERE id = ?").bind(payload.user.id).first();
     if (!user) {
       console.warn("[email-plugin] auth:password-reset:requested: user not found", {
         userId: payload.user.id
@@ -1357,7 +1357,7 @@ If you didn't change your password, contact support immediately at ${supportEmai
 function makeOnPasswordResetCompleted(env) {
   const db = env.DB;
   return async (payload) => {
-    const user = await db.prepare("SELECT id, email, first_name FROM users WHERE id = ?").bind(payload.user.id).first();
+    const user = await db.prepare("SELECT id, email, first_name FROM auth_user WHERE id = ?").bind(payload.user.id).first();
     if (!user) {
       console.warn("[email-plugin] auth:password-reset:completed: user not found", {
         userId: payload.user.id
diff --git a/node_modules/@sonicjs-cms/core/dist/index.js b/node_modules/@sonicjs-cms/core/dist/index.js
index 122d570..bbbf1e8 100644
--- a/node_modules/@sonicjs-cms/core/dist/index.js
+++ b/node_modules/@sonicjs-cms/core/dist/index.js
@@ -1227,7 +1227,7 @@ function makeOnRegistrationCompleted(env) {
       if (setting?.value === "true") return;
     } catch {
     }
-    const user = await db.prepare("SELECT id, email, first_name FROM users WHERE id = ?").bind(payload.user.id).first();
+    const user = await db.prepare("SELECT id, email, first_name FROM auth_user WHERE id = ?").bind(payload.user.id).first();
     if (!user) {
       console.warn("[email-plugin] auth:registration:completed: user not found", {
         userId: payload.user.id
@@ -1299,7 +1299,7 @@ var DEFAULT_EXPIRY_MS = 60 * 60 * 1e3;
 function makeOnPasswordResetRequested(env) {
   const db = env.DB;
   return async (payload) => {
-    const user = await db.prepare("SELECT id, email, first_name FROM users WHERE id = ?").bind(payload.user.id).first();
+    const user = await db.prepare("SELECT id, email, first_name FROM auth_user WHERE id = ?").bind(payload.user.id).first();
     if (!user) {
       console.warn("[email-plugin] auth:password-reset:requested: user not found", {
         userId: payload.user.id
@@ -1368,7 +1368,7 @@ If you didn't change your password, contact support immediately at ${supportEmai
 function makeOnPasswordResetCompleted(env) {
   const db = env.DB;
   return async (payload) => {
-    const user = await db.prepare("SELECT id, email, first_name FROM users WHERE id = ?").bind(payload.user.id).first();
+    const user = await db.prepare("SELECT id, email, first_name FROM auth_user WHERE id = ?").bind(payload.user.id).first();
     if (!user) {
       console.warn("[email-plugin] auth:password-reset:completed: user not found", {
         userId: payload.user.id
